# StreamingApp DevOps Deployment Project #

## Project Overview

This project demonstrates end-to-end orchestration, CI/CD automation, containerization, and scalable deployment of a MERN-based microservices application using AWS and DevOps tools.

The application was containerized, integrated with Jenkins CI/CD, stored in Amazon ECR, and deployed to Amazon EKS using Kubernetes.

---

# Tech Stack

* Frontend: React.js
* Backend: Node.js / Express.js
* Architecture: Microservices
* Containerization: Docker
* CI/CD: Jenkins
* Container Registry: Amazon ECR
* Orchestration: Kubernetes (Amazon EKS)
* Package Manager: Helm
* Monitoring: Amazon CloudWatch
* Cloud Provider: AWS

---

# Project Repository

Original Repository:
https://github.com/UnpredictablePrashant/StreamingApp

Deployment / Submission Repository:
(Add your GitHub repository link here)

---

# Project Architecture

```text
Developer Pushes Code to GitHub
            ↓
        Jenkins Pipeline
            ↓
 Build Docker Images for Services
            ↓
 Push Images to Amazon ECR
            ↓
 Deploy to Amazon EKS
            ↓
 Access Application via LoadBalancer
```

---

# Microservices Used

* auth-service
* chat-service
* admin-service
* streaming-service
* streaming-frontend

---

# Implementation Steps

## Step 1: Source Code Management

* Forked / cloned the project repository from GitHub.
* Managed project source code using Git.

## Step 2: Dockerization

Created Docker images for:

* auth-service
* chat-service
* admin-service
* streaming-service
* streaming-frontend

Sample command:

```bash
docker build -t auth-service ./backend/authService
```

---

## Step 3: Amazon ECR Setup

Created repositories in Amazon ECR and pushed images.

Sample commands:

```bash
aws ecr create-repository --repository-name auth-service

docker push <account-id>.dkr.ecr.ap-south-1.amazonaws.com/auth-service:latest
```

---

## Step 4: Jenkins CI/CD Pipeline

Installed Jenkins on AWS EC2 instance.

Pipeline stages:

* Clone Source Code
* Build Docker Images
* Authenticate with ECR
* Push Images to ECR

---

## Step 5: Kubernetes Deployment using Amazon EKS

Created EKS cluster using eksctl.

```bash
eksctl create cluster --name streaming-cluster-v2 --region ap-south-1 --nodes 2 --node-type t3.medium
```

Verified cluster:

```bash
kubectl get nodes
```

---

## Step 6: Application Exposure

Frontend deployed using Kubernetes Service type LoadBalancer.

```bash
kubectl expose deployment streaming-frontend --type=LoadBalancer --port=80 --target-port=80
```

Accessed application successfully through external browser URL.

---

# Monitoring & Logging

* CloudWatch can be integrated for metrics and logs.
* Kubernetes resources monitored using kubectl commands.

---

# Validation Completed

* Jenkins pipeline executed successfully
* Docker images built successfully
* Images pushed to ECR
* EKS cluster created successfully
* Pods running successfully
* Services exposed successfully
* Application accessible in browser

---
# Commands Used (Brief)

## Git Commands

```bash
git clone https://github.com/UnpredictablePrashant/StreamingApp.git
cd StreamingApp
git status
git add .
git commit -m "Project updates"
git push origin main
```

---

## Docker Commands

```bash
docker build -t auth-service ./backend/authService
docker build -t chat-service ./backend/chatService
docker build -t admin-service ./backend/adminService
docker build -t streaming-service ./backend/streamingService
docker build -t streaming-frontend ./frontend

docker images
docker ps
```

---

## AWS CLI Commands

```bash
aws configure
aws sts get-caller-identity
```

---

## Amazon ECR Commands

```bash
aws ecr create-repository --repository-name auth-service
aws ecr create-repository --repository-name chat-service
aws ecr create-repository --repository-name admin-service
aws ecr create-repository --repository-name streaming-service
aws ecr create-repository --repository-name streaming-frontend

aws ecr get-login-password --region ap-south-1 | docker login --username AWS --password-stdin <account-id>.dkr.ecr.ap-south-1.amazonaws.com
```

---

## Docker Push Commands

```bash
docker tag auth-service:latest <account-id>.dkr.ecr.ap-south-1.amazonaws.com/auth-service:latest
docker push <account-id>.dkr.ecr.ap-south-1.amazonaws.com/auth-service:latest
```

(Same process repeated for all services)

---

## Jenkins Commands

```bash
sudo apt update
sudo apt install openjdk-17-jdk -y
sudo apt install jenkins -y
sudo systemctl start jenkins
sudo systemctl enable jenkins
sudo systemctl status jenkins
```

---

## Docker on Jenkins Server

```bash
sudo apt install docker.io -y
sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker jenkins
sudo usermod -aG docker ubuntu
```

---

## Kubernetes / EKS Commands

```bash
eksctl create cluster --name streaming-cluster-v2 --region ap-south-1 --nodes 2 --node-type t3.medium

kubectl get nodes
kubectl get pods
kubectl get svc
```

---

## Deployment Commands

```bash
kubectl create deployment streaming-frontend --image=<account-id>.dkr.ecr.ap-south-1.amazonaws.com/streaming-frontend:latest

kubectl expose deployment streaming-frontend --type=LoadBalancer --port=80 --target-port=80
```

---

## Helm Commands

```bash
helm version
helm create streamingapp
```

---

## Cleanup Commands

```bash
eksctl delete cluster --region ap-south-1 --name streaming-cluster-v2
```

# Screenshots

Add screenshots in a `screenshots/` folder and reference them here.

Examples:

* Jenkins Successful Build
* ECR Repositories
* kubectl get nodes
* kubectl get pods
* kubectl get svc
* Application UI
* EKS Cluster Dashboard

---

# Cleanup Commands

```bash
eksctl delete cluster --region ap-south-1 --name streaming-cluster-v2
```

Terminate Jenkins EC2 instance from AWS Console after submission.

---

# Conclusion

This project successfully demonstrates real-world DevOps practices including containerization, CI/CD automation, Kubernetes orchestration, scalable deployment, and cloud-native application hosting using AWS services.
